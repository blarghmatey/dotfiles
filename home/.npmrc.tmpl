# npm configuration — generated from home/.npmrc.tmpl via `dots sync`
# Edit the template, not this file.

# Auth token — stored in pass(1) under "npmjs-token"
//registry.npmjs.org/:_authToken={{ pass:npmjs-token }}

# Install global packages to a user-owned prefix (no sudo needed)
prefix=/home/tmacey/.npm_packages/

---
# yamllint configuration
# Relaxed for dotfiles: allows broader line lengths and both quote styles.

extends: default

rules:
  line-length:
    max: 120
    level: warning
  truthy:
    allowed-values: ["true", "false"]
    check-keys: true
  comments:
    min-spaces-from-content: 1
